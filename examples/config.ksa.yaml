service:
  host: "0.0.0.0"
  port: 8080

issuer: "https://keypebble.example.com"
default_ttl_seconds: 3600

# KSA tokens should use RS256 — Kubernetes OIDC validation requires asymmetric keys.
# HS256 works for local testing (replace algorithm and use hs256_secret instead).
algorithm: "RS256"
rs256_private_key: "/etc/keypebble/keypebble-private.pem"
