services:
  keypebble:
    image: keypebble:latest
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: keypebble
    command: >
      keypebble serve
      --config /etc/keypebble/config.yaml
      --policy /etc/keypebble/policy.yaml
    ports:
      - "8080:8080"
    volumes:
      - /etc/keypebble:/etc/keypebble:ro
    restart: unless-stopped
    networks:
      - keypebble_net
