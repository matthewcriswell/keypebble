service:
  host: "0.0.0.0"
  port: 8080

issuer: "https://keypebble.local"
audience: "keypebble-edge"
default_ttl_seconds: 3600
hs256_secret: "change-me"

## Optional global metadata to embed in all tokens
#static_claims:
#  environment: "dev"
#  project: "keypebble"
